<h1>Users</h1>

<ul>
    <%= @users.each do |user|%>
        <li>
            <h2><%= user.username %></h2>
            <p><%= user.email %></p>
            <%= link_to "Edit User", edit_user_path(@user) %>
        </li>
    <% end %>
</ul>

<%= link_to "Add New User", new_user_path %>